{
  "name": "",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "crawl": "node scripts/crawl-wordpress.mjs",
    "validate:seo": "node scripts/validate-seo.mjs",
    "report": "node scripts/generate-migration-report.mjs",
    "migrate": "npm run crawl && node scripts/generate-legacy-redirects.mjs && npm run report",
    "redirects": "node scripts/generate-legacy-redirects.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6",
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3"
  }
}
