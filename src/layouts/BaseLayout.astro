---
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import LineStickyBar from '../components/LineStickyBar.astro';
import SEOHead from '../components/SEOHead.astro';
import type { PageRecord } from '../lib/types';

interface Props {
  page: PageRecord;
  bodyClass?: string;
}

const { page, bodyClass = '' } = Astro.props;
---

<!doctype html>
<html lang="th">
  <head>
    <SEOHead page={page} />
    <link rel="stylesheet" href="/styles/wp-theme.css" />
    <link rel="stylesheet" href="/styles/global.css" />
  </head>
  <body class={`site-body has-line-bar ${bodyClass}`.trim()}>
    <a class="skip-link" href="#main">ข้ามไปเนื้อหา</a>
    <Header />
    <main id="main" class="site-main">
      <slot />
    </main>
    <Footer />
    <LineStickyBar />
  </body>
</html>
